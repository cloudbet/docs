# Event Tree
<%= partial 'includes/php/event_tree' %>

The Event Tree end point will allow you to discover all the competitions we have available for all sports.

It is exposed as a navigable tree to be easy to parse.